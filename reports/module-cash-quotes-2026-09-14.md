# Saved cash quotations

Desk sales staff need retained, printable versions of the cash price and customer/car details. Store immutable snapshots as note evidence (cash_quote_saved), never finance quote evidence or proof of presentation. Reuse deal_evidence hash chain and existing RLS, lock deal while saving, audit every new version, deduplicate identical current content and reject stale previews. Read and print only inside authenticated tenant/branch scope. No emails, contracts, invoice numbering, stock or deal state changes. Only simple GBP cash deals before contract, without finance/PX/add-ons/deposits; refuse below-advertised prices until discount approvals are implemented. Acceptance: snapshot immutability, exact pence, concurrent/idempotent save, stale/permission/tenant/branch refusal, readable print view, no finance evidence. Rollback routes/actions; retain evidence. No migration.


## Verification checkpoint
Implemented retained cash quote snapshots in the existing hash-chained evidence ledger, authenticated review/history page, print-only document styling and idempotent server save. Four new real-Postgres integration cases cover tenant/branch/permissions, unsupported settlement and discount gates, stale review, concurrency and version retention. Full regression suite passes: 1,843 tests across 67 files. It exposed and fixed a pre-existing regression where legacy opening_hours={} crashed public saved-car/search pages; public and CRM loaders now normalize that empty shape to an empty schedule.

Browser created a local test deal for Local Appointment Test / WK23VBO at £18,995 and saved/reopened its first quotation. Mobile light/dark automated WCAG A/AA checks pass with no page overflow. No quotation was sent, no contract formed and no live transaction performed. Print button and print styling are implemented; physical printing/PDF output has not been verified. Discount approvals and full settlement/order documents remain separate modules. No migration. Rollback these entry points; preserve all saved evidence.

Final checks: CRM image forecourt-crm-codex:cash-quotes and public image forecourt-site-codex:hours-compatibility built successfully. Typecheck/lint and the 103-table RLS gate passed. Desktop axe also passed with no overflow. Temporary QA files removed. Local public port 3100 updated with previous container retained; CRM dev at 3101 serves the new module. Changes remain uncommitted.
