# Website management
Dealer managers editing their shopfront need accurate saved-settings previews and reliable contact/hours changes. Reuse public homepage data and rendering, preserve unedited opening days/address metadata, validate email/time/copy limits, audit the complete change and make saving resilient. No publishing rules, finance or consent changes. No provider calls. Acceptance: preview reflects tenant live/reserved stock; private stock stays absent; invalid contact/hours fail before writes; Sunday data survives editing; errors preserve form inputs; permission and tenant boundaries remain enforced. No migration. Rollback application code; retained settings use the existing schema.

Implemented and verified: 1,783 tests across 60 files, lint and typecheck passed; production CRM and site Docker images built (`website-manager`). Four new integration checks cover public/private/tenant stock visibility, validation, preservation/auditing, and permission/site boundaries. Browser: isolated website-only test account; Studio preset changes fields; invalid weekday closing time keeps all edits; valid save reflected in preview and separately running public Docker service. Preview shows 14 cars and minimum £5,995; phone view has no document overflow (359px content/scroll width). Restored Classic theme and empty custom headline after testing.

No schema changes or Railway operations. Public caching policy remains five-minute shared-cache freshness with stale-while-revalidate; local fresh-origin result was verified, not Railway CDN invalidation. Preview displays saved settings, not unsaved drafts. Current hours editor still groups weekdays and Saturday; Sunday/other unedited days are retained rather than exposed as editable fields. Cross-service R2/logo storage and automated axe were not exercised. Preview intentionally disables links/forms and scripts through inert content plus an empty sandbox.
