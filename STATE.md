# STATE — Forecourt

> **Read this first, every session.** It is the single source of truth for what is done, in flight and blocked.
> Update it at the end of every session. Keep it short; detail goes in `reports/`.

**Last updated:** 2026-08-03 · by: Claude (M9–M12 build session)
**Current phase:** M0–M12 built. Three modules await external sign-off (see `docs/10-compliance-signoff-brief.md`).
**Autonomy:** Broad (see `docs/09-manager-charter.md`)

---

## Right now

| | |
|---|---|
| **Current module** | **The numbered backlog is finished except M15, which is contract-blocked on cap hpi.** What remains is UI depth — most modules are schema and domain with no screen — plus the blocked halves of M4, M16 and M17, and the M0 audit tool web front end. |
| **Last shipped** | M19 compliance centre and M20 platform admin & billing (5 Aug 2026). **1,487 tests green, lint 0, typecheck 0, 96 tables protected.** |
| **Backlog complete** | 20 of 21 modules (95%) — only M15 remains, and it is contract-blocked |
| **Repository** | **`github.com/AbdullahUmer007/forecourt`** (private, `main`, 48 files, pushed 2026-08-01) |
| **Blocking issue** | None. GitHub push access is **permanently unavailable** on this plan — see "Delivery method" below. Sessions deliver `git bundle` files; Abdullah pulls and pushes. This is settled, not a workaround. |
| **Next milestone** | Kennington live. Code is ready: our generated site now scores **100/100** on our own audit. The remaining gate is the FCA compliance consultant's sign-off of `compliance_rules` `conc.representative_example` v2, which ships **unsigned** and which nothing renders without. |

---

## Module status

| # | Module | Status | Notes |
|---|---|---|---|
| M0 | Dealer Site Audit tool | ✅ done | 16 checks, fixture mode, Kennington fixture scores 16/100. Web front end still to build. |
| M1 | Foundation | ✅ done | Workspace, tokens, domain (11 tests green), db/RLS, isolation suite, skills committed |
| M2 | Tenancy & identity | ✅ done | 13 tables, 9 roles, permissions with derived-value protection, provisioning + go-live checklist. **Fixed a cross-tenant leak in the M1 RLS generator** — see reports/session-2026-08-01-m2.md |
| M3 | Vehicle core | ✅ done | 4 tables, 15-state lifecycle with go-live gating, days metrics, advert strength. Registration is `UNIQUE (tenant_id, registration)` — verified two dealers can hold the same plate. Corrected `delivered` from terminal: a CRA rejection must be able to return it. |
| M4 | Vehicle data & provenance | 🟨 half done | **Free half BUILT**: adapter framework (cache, cost metering, circuit breaker, idempotency, raw-response storage, fixture replay), DVLA VES + DVSA MOT adapters, registration handling, mileage anomaly detection, 3 tables. **Paid half still blocked**: valuation (cap hpi) and provenance (HPI Check) need contracts — the interface and columns are ready for them. |
| M5 | Media pipeline | ✅ done | Guided capture plan (15 shots, 5 required), ordering and hero rules, responsive variant plan (AVIF/WebP/JPEG × 5 breakpoints), tenant-prefixed content-hashed storage keys, processing pipeline with mandatory EXIF stripping, upload validation by magic bytes, disclosure-evidence rules. 2 tables. `published_photo_count` is now maintained by a DB trigger — M3's go-live gate depends on it. |
| M6 | Public website engine | ✅ done | **M6a BUILT — the SEO core.** Slugs, canonical URLs, sitemap from live stock, robots.txt with a live-host directive, sold-vehicle 301 resolution, landing-page thin-content rules, per-vehicle titles and descriptions, JSON-LD (Car/Offer/AutoDealer/BreadcrumbList/ItemList). **Our generated site scores ≥85 on our own audit; the competitor scores 16.** **M6b BUILT:** host-to-tenant resolution (unknown or unverified host 404s, never falls through), theme tokens as CSS custom properties, the VDP renderer, Next.js middleware plus route handlers for the page, sitemap.xml and robots.txt. **The self-audit now runs against REAL rendered HTML, not a fixture**, and enforces zero-JS and page-weight budgets. **Design review applied 2 Aug** — GB plate, cash-price label with price-drop, provenance as a stated outcome, named declared marks, EV battery health with context, zero-based mileage chart. **`src/data/*` BUILT 2 Aug** — Postgres-backed loaders for vehicles, search, facets, finance and the demand signal, every read inside `withTenant` so RLS decides visibility. Tenant resolution moved out of middleware (Edge has no TCP, so it cannot reach Postgres) into `requireTenant`, the single source of a tenant id. Routes are now route handlers, not page components — the renderers return whole documents and a React page would nest one inside another. **Redesigned 3 Aug from the Claude Design canvas** (`design/canvas/*.dc.html`): a real masthead with opening status, a display type scale (price at up to 76px against a previous ceiling of 28px), section planes instead of uniform grey cards, declared condition full-bleed on the dark plane, and the finance block rebuilt so the APR leads at up to 104px. **The home page is BUILT** — search-first hero, live stock counts, trust before the grid. Outstanding: block editor, domain verification flow, analytics with consent mode. |
| M7 | Public inventory experience | ✅ done | Faceted search with **crawl control as a first-class feature** — a small allow-list of indexable URL shapes and `rel="nofollow"` on the rest, so ten facets do not become 9.7m near-duplicate URLs. Canonical drops the sort, keeps the page. Zero-JS results page: facets, sorting, pagination and saving all work without a script. Zero-result relaxation ladder, demand-signal capture (append-only, partitioned), shortlists with anonymous→account merge, saved searches (**now sending — M9 supplies the consent record**). 4 tables. Monthly-payment search is **blocked by an assertion** until M8. |
| M8 | Finance display & compliance | ✅ built · ⛔ **NOT LIVE** | The `ApprovedPromotion` gate: a cost-of-credit figure cannot reach a screen without a signed-off rule, an approved in-date example and reconciling arithmetic — enforced by the type system, the renderer, the database and a golden-file test. APR verification by the CCD formula (we verify, we do not originate). Cost-of-credit language scanner blocking publish. The 51% representative-APR governance report. Monthly-payment search unlocked by the same proof. 5 tables + `compliance_rules` extended. **The seeded rule is UNSIGNED and nothing renders until the retained consultant signs it.** |
| M9 | Contacts & consent | ✅ done | Consent as an **append-only record** derived at a point in time, never a boolean — `consentPosition(channel, history, asAt)` with `asAt` required, so nobody gets the scheduling-time answer to a send-time question. PECR reg. 22 soft opt-in as a four-part test reporting every failure; legitimate interest refused for email/SMS/WhatsApp. Suppression append-only with an `active` flag so a re-subscribe keeps both halves of the story. Dedupe that will not auto-merge a name-and-postcode match. Merge re-points consent and unions vulnerability flags. Partial erasure with a stated basis for anything retained. 6 tables, 55 domain tests. **M7's saved-search alerts now actually send** via `canSendAlertWithConsent`. |
| M10 | Leads & communications | ✅ done | Lead inbox, pipeline with **mandatory structured loss reasons** (refused in code and by CHECK constraint), SLA measured from the first outbound message rather than a button, marketplace parsers that triage instead of throwing, and the outbound gate delegating to M9's `canSend` so there is one implementation of "may we contact this person?". `messages` cites the consent record **by id**, enforced by a CHECK. 4 tables, 31 domain tests + a 14-test golden gate. |
| M11 | Money | ✅ built · ⚠️ **needs VAT specialist sign-off before go-live** | Invoicing with **gapless numbering from a locked counter row** (a Postgres SEQUENCE cannot be used — it does not roll back, so an aborted transaction leaves the exact gap the rule prevents). Cancellation is a credit note with its own number, never a released one. `invoices` is content-frozen on issue rather than blanket append-only, because an invoice has a lawful draft→issued→paid lifecycle. **The margin-scheme rule is enforced at four layers** — construction, guard, two CHECK constraints, and a golden test that renders a document and greps it. The VAT stock book with all 12 fields, a severity-graded health report, and period VAT summed per entry so a loss never offsets another vehicle. AML cash threshold from `compliance_rules` keyed on the receipt date, with registered and unregistered dealers getting opposite answers. 7 tables, 62 domain tests + 11 golden. |
| M12 | Deals & Evidence Ledger | ✅ built · ⚠️ **needs FCA sign-off before go-live** | **The append-only, HASH-CHAINED evidence ledger** — each entry hashes its predecessor, so tampering is provable by someone who does not trust us, which is the person who will be checking. Canonical JSON so the hash is reproducible in another language years later. `verifyChain` reports every problem because the pattern is the diagnostic. The export bundle carries a FAILING verification rather than refusing. Contract formation mandatory before contracting (decides the 14-day CCR right, wrong in both directions if defaulted). Add-ons refused if accepted before offered — the data shape of a pre-ticked box. Repair start dates frozen, completion permitted. 6 tables, 55 domain tests + 5 database constraint tests. |
| M13 | Part-exchange appraisal | ✅ done | Structured tap-to-mark damage map (46 panels → 8 costing groups), recon estimate from versioned tenant standard costs, valuation panel, offer with an internal breakdown, settlement tracking and conversion to stock with zero re-keying. 7 tables, 66 protected. **A mark with no standard cost is reported, never priced at zero** — a silent zero makes a damaged car look clean and inflates the allowance by exactly the repair bill. **The purchase price is the ALLOWANCE, not the market value**, because that is the figure the VAT stock book carries; an over-allowance is carried visibly rather than re-basing. `customerFacingOffer` is BUILT rather than filtered, so cost data has nowhere to leak from. A VAT-registered seller does not by itself make the car qualifying — the code refuses to guess and blocks conversion until a human states whether a VAT invoice was issued. 85 domain tests + 21 isolation. |
| M14 | Prep pipeline | ✅ done | Configurable stage board, job cards linked to M3 vehicle costs rather than duplicating money, parts tracking, SLAs, MOT advisories as suggested work, the photography gate. 6 tables, 72 protected. **The module turns on one distinction: BLOCKED time reported separately from WORKING time, with overlapping blocks MERGED** — a car waiting on a part and an approval at once sat for one day, not two, and an unmerged total can exceed the time the car has been in stock. Property-tested three ways. SLA measured against working time, so a supplier back-order is not the workshop's failure. **The board is built and runs** at /prep, single-column on mobile per AC1. 55 domain tests + 11 integration + 18 isolation. |
| M15 | Pricing intelligence | ⬜ todo | |
| M16 | Channel feeds | 🟨 half done | **Mapping half BUILT**, split the same way M4 was. One canonical vehicle, eight versioned adapters (Auto Trader, eBay Motors Group, CarGurus, Carwow, Meta, Google Vehicle Ads, XML, CSV) with per-channel validation, publish preview, publish rules, delisting deadlines, feed health and retry policy. 5 tables, 77 protected. **A cost-of-credit figure cannot reach a feed** — a portal renders our payload on their page with nowhere to attach the representative example, so `assertNoFinanceInFeed` runs inside a wrapper every adapter is constructed with. M3's go-live gate applies to feeds too. **Delisting is a stored deadline with a partial index**, so a sold car still advertised is a query rather than a job someone hopes ran. 60 domain tests + 18 isolation. **Wire half blocked**: pushing to Auto Trader needs partner status; the adapters, idempotency keys and sync log are ready for it. |
| M17 | Accounting sync | 🟨 half done | **Mapping half BUILT**, split like M4 and M16. Canonical chart of 12 account keys, double-entry postings for invoices, credit notes and payments, account/tax-rate mapping with a recorded agreement, dry run, error queue with retry policy, and the CSV fallback. 4 tables, 84 protected. **A margin sale posts NO output VAT on the invoice AND a separate journal for the VAT on the margin** — both mandatory, each wrong without the other, and  returns the pair or refuses. Nothing posts to an unmapped account. Double entry enforced by a CHECK as well as in code. **The CSV fallback needs no integration at all** — a dealer whose accountant uses something unsupported can hand over a file today. 42 domain tests + 12 isolation. **Wire half blocked**: Xero/QuickBooks/Sage need OAuth apps; the postings, mapping and idempotency keys are ready for them. |
| M18 | Reporting & Channel P&L | ✅ done | **The Channel P&L**, which the spec calls the most persuasive screen in the product, plus attribution, the owner dashboard tiles, drill-through and CSV export. 3 tables, 87 protected. **A deal with no lead is UNATTRIBUTED, never credited to the website** — the tempting wrong answer, and the one that would flatter us specifically. A sale is credited to the lead it was worked from with assisting channels named beside it, not a weighted model nobody can audit. **No figure is ever Infinity or NaN** (property-tested), and below six sales the ROI is not stated at all. Per-unit figures round half-up through the tested `multiply` path — the spec's own worked example is what a dealer checks with a calculator. `channel_costs` holds actual spend separately from M16's contract rate, because a P&L a bank statement can disprove loses the argument it exists to win. 35 domain tests + 9 isolation. |
| M19 | Compliance centre | ✅ done | Compliance dashboard scoring, DISP complaint clocks, the 72-hour ICO breach clock, registers with expiry, evidence completeness per deal. 4 tables, 96 protected. **A compliance statement cannot be constructed without a source citation** — FCA Handbook reference, HMRC notice or legislation, so a dealer’s adviser can check our interpretation; the mandatory disclaimer travels on every statement. **The score never counts an unassessable area as a pass**, which is how every flattering compliance dashboard reads 100% for a dealer who has recorded nothing. 40 domain tests + 12 isolation. |
| M20 | Platform admin & billing | ✅ done | Tenant directory and health, Stripe-shaped subscriptions with dunning, stock-band plans, feature flags, per-tenant usage quotas, and **support impersonation**. 5 tables. Impersonation is the most dangerous feature in the product and every safeguard is a REFUSAL: a live tenant grant, a real reason, a bounded window capped at four hours, and a second person for commission data — enforced by a CHECK constraint (), not only in code. The session log is content-frozen, because a log we could edit would be worth nothing. Dunning never withholds the stock book or VAT records. 39 domain tests + 15 isolation. |

---

## Needs Abdullah (ranked)

1. **The FCA compliance consultant is now the single thing between us and a live Kennington site.** M8 is built and passes every test, and it will not render a payment figure for anybody until a named person signs off `compliance_rules` → `conc.representative_example` v2. What they are being asked to approve is one reviewable data row — the required fields, their order, which one gets prominence, the 51% threshold and the 90-day re-approval window — not a codebase. Budget ~£1.5–3k/month; also needed for M11 (VAT specialist) and M12.
   **Ask them specifically about CP26/15**: the consultation on removing the representative example and revisiting the 51% threshold closed on 17 June 2026 and no policy statement had been published as at 2 August. Their view on the likely landing point decides whether we build the alternative disclosure now or wait.
3. **Start the data provider conversations** — cap hpi / HPI Check (same parent, negotiate together), an aggregator for launch speed, Auto Trader technology partner status. All sales-gated, months of lead time, and they gate M4 and M16.
4. **Confirm the Kennington numbers** before any pitch: units per month, current finance penetration, average commission, what they actually pay today.
5. **Decide the product name and buy the domain.** "Forecourt" is a working name — check trade marks in classes 9 and 42.
6. **Approve the two published guarantees** (Data Portability Charter, 90-Day Switch Guarantee) and get the terms drafted properly.

---

## Running it locally

```bash
cp .env.example .env          # DATABASE_URL → your local Postgres 16
pnpm install
pnpm db:setup                 # extensions, RLS, all 6 migrations
pnpm db:policies              # asserts all 36 tables are protected
pnpm db:seed                  # Kennington demo tenant, 14 cars
pnpm dev                      # http://localhost:3000 (PORT=3001 to move it)
```

**The CRM** (`apps/crm`) needs its own demo data and a development identity:

```bash
pnpm db:seed:crm              # staff, roles, two appraisals with M13 data
# .env → CRM_DEV_USER=owner@kenningtoncarsales.co.uk
pnpm dev:crm                  # http://localhost:3002
```

```bash
pnpm db:seed:prep             # ten default stages + five cars on the board
```

Sign in with either seeded account — the password is printed by
`db:seed:crm`, and both use the same one:

| Account | Sees |
|---|---|
| `owner@kenningtoncarsales.co.uk` | Cost prices, and can record damage marks |
| `sales@kenningtoncarsales.co.uk` | Neither the offer breakdown nor the trade valuation |

Those two are worth comparing: the sales executive has no
`vehicle.cost.read`, so the market value, recon, target margin and the trade
figure are **never sent to the browser** rather than hidden in it.

`pnpm preview` writes `demo/preview/*.html` — the same renderers over the same
data, as files you can open from disk with no database at all.

The finance block is **absent by default and that is correct**: the seed leaves
`conc.representative_example` unsigned. `DEMO_SIGN_COMPLIANCE_RULE=1 pnpm db:seed`
writes a demo sign-off (rule version 999) to see it.

**Demo data:** `demo/kennington.ts`. Kennington's real public business facts
(name, address, phones, FRN 993469) with invented stock and generated
placeholder photographs — none of their images or listing copy. Drop a
`demo/kennington-stock.json` in the same shape to replace the stock.

---

## Delivery method — SETTLED, do not revisit

**Sessions cannot push to GitHub. Do not attempt it, do not test for it, and never ask Abdullah to fix it.**

Investigated exhaustively on 1–2 Aug 2026 and closed:
- The GitHub OAuth connector works — a session can authenticate and read the account identity as `AbdullahUmer007`.
- The Claude GitHub App was correctly installed against `AbdullahUmer007/forecourt` on 1 Aug.
- Every API call to `/repos/AbdullahUmer007/forecourt` still returns **403 "GitHub access to this repository is not enabled for this session"**, in three separate sessions including a freshly-fired one.
- The `add_repo` tool the error message names **does not exist in Cowork sessions** — it is a Claude Code feature.
- The session-to-repository binding is an **organisation-level setting**, and organisation settings require a **Team or Enterprise plan**. This account is on an individual plan, so the control is not available. Confirmed by Abdullah on 2 Aug.

**The workflow (improved 2 Aug — the local folder is now connected):**

Abdullah connected `D:\Projects\dealer\forecourt` as a device folder, so a
session can write **directly into his working copy**. This is now the preferred
path — no download, no bundle, no pull.

1. Build and commit locally in the session container (keeps a clean history).
2. `SendUserFile` each changed file to get a `file_uuid`.
3. `device_commit_files` to `D:\Projects\dealer\forecourt\<path>`.
4. Abdullah runs `git add -A && git commit && git push`.

**⚠️ NEVER run git via `device_bash`. Not even `git status`.**

`device_bash` cannot delete files, so ANY git command that takes a lock —
including `git status` and `git diff` — leaves a stale `.git/index.lock`
behind that it cannot clean up. That jams the repository, and Abdullah has to
`rm -f .git/index.lock` before he can commit anything. This happened on
2 Aug and cost him a confusing failure.

Safe on the device folder: reading FILES (`cat`, `ls`, `grep`, `head`).
Unsafe: anything starting with `git`.

To learn the repo state, read `.git/HEAD` and the files directly, or just ask.
All git operations — status, add, commit, push — are Abdullah's, in his own
terminal.

Fall back to `git bundle` only for very large changesets where per-file
commits would be tedious.

**Consequences to hold in mind:**
- A session **cannot see the true state of `main`**. `STATE.md` in the project is the authoritative record — trust it over any assumption about the remote.
- Always state in the report which commits the bundle contains, so Abdullah can tell what is outstanding.
- Never assume a previous session's work reached GitHub. It only did if Abdullah pulled and pushed it.

---

## Open risks

| Risk | Status |
|---|---|
| Sessions cannot push to GitHub (plan-tier limitation) | **Closed — accepted permanently.** Bundle delivery is the workflow. The residual risk is drift between the container's copy and `main`; mitigated by treating project `STATE.md` as authoritative and naming the bundle's commits in every report. |
| Line endings: Windows checkout was CRLF against an LF repo, showing all 71 files as permanently modified. Fixed 2 Aug with `.gitattributes` (`text=auto eol=lf`). Any future "everything is modified" report is this recurring — check `git diff --ignore-all-space` first. |
| Git credentials on the founder's machine default to a different GitHub account (`naumansharifwork`) | The `forecourt` remote URL now pins `AbdullahUmer007@`, so pushes from `/d/Projects/dealer/forecourt` are correctly attributed. Other repos on that machine are unaffected and may still push under the cached account. |
| FCA motor finance redress scheme partially suspended (Upper Tribunal, ~1–2 July 2026; hearing expected Dec 2026–Feb 2027) | Monitoring monthly. All scheme parameters held as data, not code. |
| FCA CP26/15 may change the CONC 3.5.3R representative example and the 51% threshold | Monitoring. `<FinancePromotion>` field list must be configurable, not fixed. |
| ~~The CRM has no authentication.~~ | **Closed 5 Aug 2026.** Argon2id passwords, session tokens hashed at rest, absolute + idle lifetimes, account lockout, the `(app)` route group as the single auth boundary — plus, as of 5 Aug: **MFA enrolment and enforcement** (a password-only session is redirected from every authenticated route; verified against the running app in all three states), recovery codes, admin-initiated password reset, and **per-IP rate limiting**, which is the hole account lockout cannot close. **One thing remains and it is honest rather than forgotten:** self-service "email me a reset link" needs an email adapter, and there is none — no Postmark, no Resend, nothing in `packages/adapters` but the vehicle lookups. The token machinery is real; the delivery is the gap. |
| ~~`pnpm lint` and `pnpm typecheck` have never run.~~ | **Closed 3 Aug 2026.** Both are real gates now and CI runs them without `\|\| true`. See DECISIONS.md. `pnpm tokens:build` is still broken — `packages/tokens/build.mjs` does not exist, so the CRM's `@theme` block is hand-copied from `tokens.json` and can drift. Small, but it is the fourth broken command found this session. |
| **Old entry, superseded:** `pnpm lint` and `pnpm typecheck` have never run. `lint` calls `eslint`, which is not installed and has no config file anywhere in the repo. `typecheck` calls `tsc -b` against a root `tsconfig.json` that does not exist — only `tsconfig.base.json` does. Vitest transpiles with esbuild and does **not** typecheck, so every module to date has shipped without a type check. | **Open.** Checked M13 by pointing `tsc` at the base config manually: the new module is clean, but the run surfaced ~12 pre-existing errors elsewhere (`exactOptionalPropertyTypes` violations in `provisioning.test.ts` and `permissions.test.ts`, a `number` widening in `media.ts:202`, and four ambiguous `export *` names in `index.ts` — a barrel silently DROPS an ambiguous name rather than failing, so those exports are missing from the package surface). Two of CLAUDE.md's build gates are currently decorative. Worth a dedicated session before M14. |
| Competitor could fix the SEO and finance gaps | Structural for them (URL routing, sitemaps, schema, a compliant finance component across 22 themes). Lead measured in months. Keep moving to compliance depth. |
| Our own site failing our own audit | **Closed 2 Aug 2026.** `tests/self-audit/generated-site.test.ts` builds a site with the real generators and runs the real audit checks against it. It now scores **100/100** with no check outstanding — the finance-display check (18 points, the heaviest) closed when M8 landed. Any regression fails the build. |

---

## Metrics

| | Now | 90-day target |
|---|---|---|
| Modules complete | 20 / 21 | 9 (M0–M8) — **well ahead** |
| Tests passing | **1,487 green** across 39 files, including 292 isolation tests against real Postgres | — |
| Tables protected by RLS | 96 | every one |
| Our own audit score | **100 / 100** (competitor: 16) | 100 |
| Runnable locally | **yes** — `pnpm db:setup && pnpm db:seed && pnpm dev` | yes |
| Dealer sites audited | 1 (Kennington, via fixture) | 500 |
| Design partners | 0 | 8 |
| Paying dealers | 0 | first in sight |
| Tenant leaks | 0 | 0 |

---

## Session log

| Date | Session | Outcome |
|---|---|---|
| 2026-08-01 | Setup | Strategy docs 01–07, three skills, design brief, M0 audit tool (16 checks, tested), M1 foundation (11 tests green), build plan, manager charter, scheduled tasks created |
| 2026-08-01 | Repo | `github.com/AbdullahUmer007/forecourt` created and pushed — 48 files, 74 objects, `main`. Claude GitHub App access still to be granted. |
| 2026-08-01 | M2 build | Tenancy, identity, roles, permissions, provisioning. 129 tests green. Found and fixed a cross-tenant leak: permissive `site_scope` policies were OR-ing away tenant isolation on every table with a `site_id`. |
| 2026-08-02 | CI fix | Three CI bugs: pnpm version conflict, a migration glob matching nothing, and `verify-policies.mjs` skipping partitioned parents and the tenants/users tables. |
| 2026-08-02 | M3 build | Vehicle core — 4 tables, lifecycle state machine with go-live gating, days metrics, advert strength. 175 tests green, 18 tables protected. |
| 2026-08-02 | M4a/b build | Adapter framework + DVLA VES and DVSA MOT (both free, no contract needed). Mileage anomaly detection now populates the field M3's go-live gate reads. 214 tests green, 21 tables protected. |
| 2026-08-02 | Delivery | Local repo folder connected — sessions now write directly into the working copy instead of shipping bundles. Fixed a CRLF/LF problem that had all 71 tracked files reading as modified. |
| 2026-08-02 | M5 build | Media pipeline — capture plan, ordering, variants, storage keys, processing, evidence rules. 263 tests green, 23 tables protected. DB now owns `published_photo_count`. |
| 2026-08-02 | M6a build | SEO core — slugs, sitemap, robots, sold-vehicle redirects, JSON-LD. **Self-audit gate added: our generated site scores ≥85 against our own tool.** Also fixed a non-idempotent isolation seed that only passed on a fresh database. 319 tests green. |
| 2026-08-02 | M6b build | Site renderer — tenant resolution, theme tokens, VDP, Next.js routes. Self-audit upgraded to run against real rendered HTML. Zero-JS and page-weight budgets now enforced by test. 358 tests green. |
| 2026-08-02 | Local build | The site runs end to end: `src/data/*` loaders on Postgres, seed script, `pnpm dev`, and a database-free `pnpm preview`. Found four real bugs doing it — an invalid `interface extends` on an indexed access, a `Tx` type that silently resolved to `never`, tenant resolution that cannot work in Edge middleware, and `loadDealer` reading WhatsApp off the wrong settings column. Also corrected the statutory definition of "total amount payable" after checking a live competitor example against our own verifier. |
| 2026-08-02 | M8 build | Finance display & compliance — the ApprovedPromotion gate, CCD APR verification, cost-of-credit language scanner, 51% governance report, golden-file acceptance test. Our generated site now scores 100/100 on our own audit. Also found two latent bugs: `compliance_rules` has had NO table grant since M1, so the app role could never read the VAT, AML or CRA rules; and M1's seeded CONC rule listed the representative-example fields in the wrong order. 600 tests green, 36 tables protected. **Not live: the rule ships unsigned.** |
| 2026-08-02 | M7 build | Public inventory experience — faceted search with crawl control, zero-JS results page, zero-result relaxation ladder, demand signal, shortlists, saved searches. 4 tables, 29 protected. Also fixed the isolation suite's Gate 1, which used `relkind = 'r'` and so silently exempted every partitioned table. 490 tests green. |
| 2026-08-03 | M12 build | Deals and the Evidence Ledger — the strategic moat. 6 tables, 59 protected. The ledger is hash-chained rather than merely append-only: append-only is a promise about our own code, the chain is a property of the data that survives a restore, a migration and an export to a third party. Every tamper vector tested — altered payload, deleted entry, swapped sequence numbers, forged insertion, back-dated check, and re-hashing the altered entry alone. Documented the honest limit: a FULL rewrite verifies, which is why the export bundle carries its own hash and date. Also wrote the consolidated sign-off brief covering all three unsigned gates. **917 tests green.** |
| 2026-08-03 | M11 build | Money — invoicing, the VAT margin stock book, payments and the AML cash threshold. 7 tables, 53 protected. Gapless numbering from a locked counter row rather than a SEQUENCE, which cannot roll back. `invoices` content-frozen on issue instead of blanket append-only, and the isolation suite rewritten to assert that GUARANTEE rather than demanding a named trigger it would have accepted doing nothing. Added the first golden-file gate: a margin invoice rendered and grepped, with the guard itself tested against three real breach shapes. Property tests assert margin VAT is never negative and a loss on one vehicle never reduces VAT on another. **Ships unsigned — the VAT specialist's review is the go-live gate, exactly as M8's is.** 845 tests green. |
| 2026-08-03 | M9 + M10 build | Contacts & consent, then leads & communications. 10 tables, all RLS-forced and registered in the leak suite (147 isolation tests, up from 126). Consent is derived from an append-only history at a stated moment, never a stored boolean; `asAt` is a required argument so nobody gets the scheduling-time answer to a send-time question. Legitimate interest refused for electronic marketing (PECR reg. 22). Dedupe refuses to auto-merge a name-and-postcode match — a father and son at one address are two people. Loss reasons mandatory in code AND by CHECK constraint. `prepareOutbound` delegates to `canSend`, with a test asserting the two agree, so there is one implementation of "may we contact this person?". Added the third golden-file gate: 14 adversarial tests enumerating every route to an unlawful send. **M7's saved-search alerts, inert since M7 shipped, now actually send.** 749 tests green. |
| 2026-08-03 | Isolation gate | `pnpm db:setup` failed on a second run (`CREATE TYPE` has no `IF NOT EXISTS`); it now detects an existing database and takes `--reset`. Running the isolation suite then exposed something worse: **it had never run on a seeded database and reported green**. `TENANT_A` was the same UUID `seed-demo.mjs` gives Kennington, so the seed collided on `brands_tenant_default_unique`, `beforeAll` threw, and all 125 tests reported as *skipped* — which every CI summary counts as success. Isolation ids moved to an `ffffffff-…` space, `.env` now loads for vitest, and a always-on test fails when the gate cannot run. **126 isolation tests now genuinely pass: zero cross-tenant leaks.** |
| 2026-08-03 | Design build | Implemented the six-page Claude Design canvas across the public site. New `chrome.ts` (masthead with server-computed opening status, footer with the FCA disclosure), a real type scale in `theme.ts`, section planes, the finance block rebuilt with the APR leading, and **the home page built** — M6's last outstanding renderer, so the root no longer 307s to the stock list. Found and fixed **two failing tests that had been left broken**: a fixture that added the deposit into "total amount payable" (blocking the entire self-audit suite from loading) and a golden file still holding the pre-correction figure. Also found a silent-content-loss bug class — `raw()`/`when()` interpolated into a plain template literal stringifies to `[object Object]` and drops the content, which had eaten the derivative and the enquiry phone number; two regression tests now assert against it. 494 tests green, self-audit still 100/100. |
| 2026-08-05 | M19 + M20 build | **The last two unblocked modules, which finishes the numbered backlog bar M15.** M19 compliance centre: DISP complaint clocks, the 72-hour ICO breach clock, registers with expiry, evidence completeness per deal, and the dashboard score. **A compliance statement cannot be constructed without a source citation** — §27.4 requires a dealer's own adviser to be able to check our interpretation, and a statement without one is an assertion we are not entitled to make about somebody else's regulatory position. The score **never counts an unassessable area as a pass**, which is how every flattering compliance dashboard reads 100% for a dealer who has recorded nothing at all. The DISP clock runs from RECEIPT and a complaint answered in time never later turns red; the breach clock runs from AWARENESS, not from the breach — a laptop taken in March and noticed in June gives 72 hours from June. M20 platform admin: tenant directory and health, subscriptions with dunning, stock-band plans, feature flags, usage quotas, and **support impersonation — the most dangerous feature in the product**, where every safeguard is a refusal rather than a warning and the four-eyes rule for commission data is a CHECK constraint rather than only application logic. Dunning never withholds the stock book or VAT records, because they are the dealer's statutory records and not ours to hold hostage. 9 tables, 96 protected, both migrations verified rolling back and re-applying in dependency order. **1,487 tests green**, verified on a pristine migrations-only database. |
| 2026-08-05 | M18 build | **Reporting and the Channel P&L** — the screen the spec calls the most persuasive in the product, because it reframes our subscription as the cheapest channel a dealer has. Which is exactly why the attribution had to be honest: **a deal with no lead is unattributed, never credited to the website**, since that wrong answer would flatter us on the one table whose whole job is being trusted in a negotiation. A sale is credited to the lead it was actually worked from, with assisting channels named beside it — deliberately not a weighted multi-touch model, because a dealer doing thirty cars a month cannot audit fractional credit and will not take an unauditable number to a marketplace. No figure is ever Infinity or NaN, property-tested across arbitrary lead/sale/spend combinations. Below six sales the ROI is not stated — the fourth place in the codebase where the honest answer is "not enough evidence yet". **Writing the test against the spec's own worked example caught our per-unit figures a penny light**: integer division truncated where £1,850 ÷ 142 = £13.03, and a dealer checks that with a calculator. Now rounded half-up through the tested `multiply` path. `channel_costs` holds actual spend separately from M16's contract rate — they differ on every promotion and overage, and a P&L a bank statement can disprove loses the argument. **1,378 tests green**, verified on a pristine migrations-only database. |
| 2026-08-05 | M17 build | **Accounting sync — the mapping half.** Split like M4 and M16: Xero/QuickBooks/Sage need OAuth apps, but the chart, the postings, the mapping, the dry run, the error queue and the CSV fallback do not. 4 tables, 84 protected, rollback verified. **The rule that carries the risk: a margin sale posts NO output VAT on the invoice AND a separate journal for the VAT on the margin.** Both are mandatory and each is wrong without the other — put VAT on the invoice and the sale becomes standard-rated (M11 enforces that at four layers); omit the journal and the dealer underpays VAT on every margin car and finds out at an inspection. `postingsFor` returns the pair or refuses. A loss-making car posts no journal, which is stated as its own test because "no journal" looks identical to a bug otherwise. Nothing posts to an unmapped account and every unmapped one is reported at once. A credit note reverses by swapping sides, never by negating — a negative debit and a credit look identical in a total and completely different in a ledger. Double entry enforced by a CHECK as well as in code, and by a property test over invoices of any size. **The CSV fallback works with no integration at all**, so a dealer whose accountant uses something unsupported can hand over a file today. Verified on a pristine migrations-only database. **1,334 tests green.** |
| 2026-08-05 | Auth hardening | **The three gaps that were blocking the CRM from a real dealer, closed.** MFA enrolment and enforcement: the gate is on the READ path in the `(app)` route group, so a session with a correct password and no second factor is redirected from every authenticated route — verified against the running app in all three states (not enrolled, enrolled but unsatisfied, satisfied). The secret is written only AFTER a code from it verifies, because enabling MFA on an unverified secret locks someone out of their own account. Ten recovery codes issued AT enrolment rather than offered later, hashed, single-use, and normalised so a code read off paper in lower case still works. **Per-IP rate limiting** — the hole account lockout cannot close, because one password sprayed across five hundred accounts trips nothing when the counter is per account. `auth_attempts` grants the app INSERT only and the limiter counts through a SECURITY DEFINER function, so a table holding every email anybody has tried cannot be enumerated. The three new tables carry no `tenant_id` by design and were added to `verify-policies.mjs`'s SPECIAL list — that file's own comment says "it has no tenant_id is precisely how a table ends up unprotected". Password reset with hashed single-use tokens that revoke every session on completion. TOTP verified against independently-computed codes, which is what proves the base32 decode. **1,280 tests green; verified on a pristine migrations-only database too.** |
| 2026-08-04 | M16 build | **Channel feeds — the mapping half.** Split like M4: labelling the whole module "blocked on Auto Trader partner status" would have idled months of buildable work. One canonical vehicle, eight versioned adapters with per-channel validation, publish preview, rules, delisting, health and retry. 5 tables, 77 protected, rollback verified. **The rule the module exists to not break: a cost-of-credit figure can never reach a feed.** A portal renders our payload on their page with nowhere to attach the representative example CONC 3.5.3R requires, so the answer is not "render it carefully" but "do not send it" — and the guard lives inside a wrapper every adapter is constructed through, so a channel added next year gets it regardless. A test asserting "every adapter refuses an APR in the headline" **failed on CarGurus, correctly**: it has no headline field, so the text is never sent there. The guard protects the payload, not the vehicle record, which is the right boundary — the breach is what a portal renders. Delisting is a stored deadline with a partial index rather than an event someone hopes fired; a reserved car is deliberately not delisted, because reservations fall through and pulling the advert costs the next buyer. Verified on a pristine migrations-only database as well as the seeded one, per the CI lesson. **1,243 tests green.** |
| 2026-08-04 | M14 build | **Prep pipeline, board included.** 6 tables, 72 protected, rollback verified against the live database. The module turns on one distinction — blocked time reported separately from working time, with overlapping blocks MERGED, because a car waiting on a part and an approval simultaneously sat for one day and an unmerged total can exceed the time the car has been in stock. Property-tested three ways: the merged total never exceeds the naive sum, never exceeds the span it covers, and merging is idempotent. The SLA is measured against working time so a supplier's back-order is not counted against the workshop. `applyMove` takes a transaction and `moveCard` takes a cookie, which is what makes eleven integration tests against a real database possible — the photography gate, exactly-one-open-stage-event, closed-at equals opened-at, the audit row, and the override path. **The board renders the right numbers on seeded data**: two overlapping 72h blocks show as 96h waiting rather than 144h, and a car with 216h waiting and 24h worked correctly shows no SLA breach. **`pnpm typecheck` caught its first regression** — `slaState` collided with `leads.ts` and the barrel was silently dropping both, the third instance of that class and the first one caught automatically. **1,168 tests green.** |
| 2026-08-03 | CRM auth + mutations | **Authentication, and the first real mutation.** Argon2id via `@node-rs/argon2`, opaque 32-byte session tokens stored as SHA-256, absolute + idle session lifetimes, lockout after 8 attempts, sign-in and sign-out audited. The policy is all in `packages/domain/src/auth.ts` and tested independently of Next — 43 tests including an RFC 4226 TOTP vector. A wrong password and an unknown email give identical answers AND take the same time, because Argon2id's ~50ms is a timing oracle if only one path pays it. The `(app)` route group is the single auth boundary, so a new page cannot forget the check. **The damage-mark write path**: permission-checked server-side, validated against the enums, priced by the same `resolveStandard` the estimate uses, photo re-encoded through sharp so EXIF cannot survive, and the insert plus its audit event in ONE transaction — with a test that rolls back and asserts neither row survives. Found three real bugs by verifying rather than assuming: a `'use server'` file may export nothing but async functions (lint and tsc both passed it; only POSTing to the action failed); my appraisal media key did not match the `t/{tenant}/` prefix `isTenantOwnedKey` guards on; and the mark form had no progressive enhancement, so it needed JavaScript on a forecourt. **1,084 tests green.** |
| 2026-08-03 | CRM app | **`apps/crm` — the first dealer-facing UI in the product.** Next.js App Router, React, Tailwind v4 driven by the design tokens, both colour modes, 44px targets, focus rings and `prefers-reduced-motion` handled once in `globals.css`. `withSession` is the CRM's single door to the database and passes the real user id and site scope, so the RESTRICTIVE `site_scope` policies apply exactly as in production. Built the M13 appraisal list and detail screens against real seeded data — damage map, recon estimate, valuation panel, offer, settlement, conversion blockers, every figure from the domain layer rather than recomputed in the view. `pnpm db:seed:crm` adds two staff with genuinely different permissions and two appraisals, one clean and one with every warning firing. **Verifying the cost-price redaction caught a real leak**: the offer breakdown was gated but the Valuation card printed the trade figure, which is the market value under another label — a sales executive was being handed the number `vehicle.cost.read` withholds. Fixed and re-verified against the rendered payload. Also fixed `pnpm lint` and `pnpm typecheck`, neither of which had ever run — see the entry below and DECISIONS.md. |
| 2026-08-03 | M13 build | Part-exchange appraisal — 7 tables, 66 protected, 208 isolation tests. The estimate reports what it cannot price instead of costing it at zero; the purchase price that reaches the stock book is the allowance rather than the market value; the customer-facing offer is built rather than filtered so cost data has nowhere to leak from; a VAT-registered seller does not by itself make the car qualifying. Also found and fixed something that had been broken since M1: **`pnpm db:migrate` was wired to `drizzle-kit migrate` with no config file in the repository and had never once run**, so the only way to apply a migration was `db:setup --reset`, which drops the schema. Replaced with a real incremental runner. Verified the rollback by running `0011_appraisals.down.sql` against the live database and re-applying it. **1,023 tests green.** `pnpm lint` and `pnpm typecheck` are still broken — see Open risks. |
| 2026-08-02 | Design review | Reviewed the Claude Design VDP against our renderer and adopted six improvements: GB plate with UK band, labelled cash price with our own price-drop history, provenance stated as an outcome (tri-state, adverse results disclosed), declared marks counted and named, EV battery health with its typical range, and a zero-based mileage chart above the MOT table. Declined three — guide-price comparison (cap hpi blocked), advisory→prep follow-through (M14), finance block (M8). 369 tests green. |
